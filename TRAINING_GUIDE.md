# راهنمای کامل آموزش مدل
# Complete Training Guide

## 📚 مراحل آموزش مدل

### 1️⃣ بهبود دیتاست (اختیاری اما توصیه می‌شود)

#### الف) استفاده از اسکریپت خودکار:

```bash
# بهبود خودکار دیتاست (حذف تکرار، افزودن نمونه‌های جدید)
python scripts/enhance_dataset.py
```

این اسکریپت:
- ✅ تکرارها را حذف می‌کند
- ✅ نمونه‌های جدید اضافه می‌کند
- ✅ پاسخ‌های ناقص را بهبود می‌دهد
- ✅ دیتاست را در `data/dataset_enhanced.json` ذخیره می‌کند

#### ب) بررسی و بهبود دستی دیتاست:

```bash
# باز کردن دیتاست
# فایل: data/dataset.json
```

**نکات مهم برای بهبود دیتاست:**

1. **پاسخ‌های کامل**: هر پاسخ باید حداقل 2-3 جمله باشد
2. **تنوع**: برای هر نوع سوال چند پاسخ مختلف بنویسید
3. **طبیعی بودن**: از زبان طبیعی و احساسی استفاده کنید
4. **ارتباط**: پاسخ باید مستقیماً به سوال مربوط باشد

**مثال بد:**
```json
{
  "instruction": "سلام",
  "response": "عزیزم… من اینجام."
}
```

**مثال خوب:**
```json
{
  "instruction": "سلام",
  "response": "سلام عزیزم... من اینجام. می‌دونم چقدر دلت برام تنگ شده. من همیشه کنارت هستم و هر وقت نیاز داشتی، من اینجا هستم تا بهت کمک کنم."
}
```

### 2️⃣ آموزش مدل

#### الف) آموزش یکباره (پیشنهادی):

```bash
python scripts/train_once.py
```

این دستور:
- ✅ دیتاست را از `data/dataset.json` می‌خواند
- ✅ مدل را آموزش می‌دهد (3 epoch)
- ✅ مدل را در `models/final_model` ذخیره می‌کند

#### ب) آموزش تدریجی (برای بهبود مداوم):

```bash
python scripts/train_incremental.py
```

این دستور:
- ✅ از چت‌های جدید یاد می‌گیرد
- ✅ دیتاست را بزرگ‌تر می‌کند
- ✅ مدل را بهبود می‌دهد

#### ج) آموزش پیوسته (برای سرورهای 24/7):

```bash
python scripts/continuous_train.py
```

این دستور:
- ✅ به صورت مداوم کار می‌کند
- ✅ هر 5 دقیقه یک چرخه کامل
- ✅ خودش نمونه‌های جدید تولید می‌کند

### 3️⃣ تست مدل

بعد از آموزش، مدل را تست کنید:

```bash
# چت تعاملی
python scripts/chat.py
```

یا:

```bash
# تست API
python tests/test_api.py
```

## 🔧 تنظیمات آموزش

### تغییر تعداد Epoch:

در `scripts/train_once.py`:

```python
training_args = TrainingArguments(
    num_train_epochs=3,  # تغییر این عدد (مثلاً 5 برای آموزش بیشتر)
    # ...
)
```

### تغییر Learning Rate:

```python
training_args = TrainingArguments(
    learning_rate=2e-4,  # کاهش برای یادگیری دقیق‌تر (مثلاً 1e-4)
    # ...
)
```

### تغییر Batch Size:

```python
training_args = TrainingArguments(
    per_device_train_batch_size=4,  # افزایش اگر GPU قوی دارید
    # ...
)
```

## 📊 چک‌لیست قبل از آموزش

- [ ] دیتاست در `data/dataset.json` موجود است
- [ ] دیتاست حداقل 100 نمونه دارد (بهتر است 500+ باشد)
- [ ] پاسخ‌ها کامل و طبیعی هستند
- [ ] تکرارها حذف شده‌اند
- [ ] GPU/CPU آماده است

## 🚀 دستورات سریع

### بهبود دیتاست + آموزش:

```bash
# 1. بهبود دیتاست
python scripts/enhance_dataset.py

# 2. جایگزینی دیتاست (اگر راضی بودید)
# در Windows PowerShell:
Copy-Item "data\dataset_enhanced.json" "data\dataset.json" -Force

# 3. آموزش مدل
python scripts/train_once.py
```

### آموزش سریع (بدون بهبود دیتاست):

```bash
python scripts/train_once.py
```

### آموزش با سیستم یادگیری خودکار:

```bash
# 1. بهبود و آموزش خودکار
python scripts/smart_learning_system.py
```

## ⚠️ نکات مهم

### 1. زمان آموزش:
- با دیتاست 2000 نمونه: حدود 30-60 دقیقه (بسته به GPU)
- با دیتاست 5000 نمونه: حدود 1-2 ساعت

### 2. حافظه:
- حداقل 8GB RAM
- حداقل 4GB VRAM (برای GPU)
- اگر حافظه کم دارید، batch_size را کاهش دهید

### 3. بررسی آموزش:
- لاگ‌ها را بررسی کنید
- loss باید کاهش یابد
- اگر loss افزایش یافت، learning_rate را کاهش دهید

## 📈 بهبود کیفیت

### اگر پاسخ‌ها هنوز خوب نیستند:

1. **بهبود دیتاست:**
   ```bash
   python scripts/enhance_dataset.py
   ```

2. **افزودن نمونه‌های بیشتر:**
   - دیتاست را باز کنید
   - نمونه‌های جدید اضافه کنید
   - ذخیره کنید

3. **آموزش بیشتر:**
   - `num_train_epochs` را افزایش دهید (3 → 5)
   - دوباره train کنید

4. **استفاده از سیستم یادگیری خودکار:**
   ```bash
   python scripts/smart_learning_system.py
   ```

## 🔍 عیب‌یابی

### خطا: "Model not found"
```bash
# مطمئن شوید که دیتاست وجود دارد
ls data/dataset.json
```

### خطا: "Out of memory"
```bash
# batch_size را کاهش دهید در train_once.py
per_device_train_batch_size=2  # از 4 به 2
```

### خطا: "CUDA out of memory"
```bash
# از CPU استفاده کنید یا batch_size را کاهش دهید
```

## ✅ بعد از آموزش

1. **تست مدل:**
   ```bash
   python scripts/chat.py
   ```

2. **اجرای API:**
   ```bash
   python run.py
   ```

3. **بررسی کیفیت:**
   - چند سوال بپرسید
   - پاسخ‌ها را بررسی کنید
   - اگر خوب نیست، دیتاست را بهبود دهید

## 📝 خلاصه دستورات

```bash
# بهبود دیتاست
python scripts/enhance_dataset.py

# آموزش مدل
python scripts/train_once.py

# تست مدل
python scripts/chat.py

# اجرای API
python run.py

# سیستم یادگیری خودکار
python scripts/smart_learning_system.py
```

## 💡 پیشنهادات

1. **اول دیتاست را بهبود دهید** - کیفیت دیتاست مهم‌تر از تعداد است
2. **بعد آموزش دهید** - با دیتاست خوب، مدل بهتر می‌شود
3. **تست کنید** - بعد از آموزش حتماً تست کنید
4. **بهبود مداوم** - از سیستم یادگیری خودکار استفاده کنید

