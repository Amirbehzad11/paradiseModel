# وابستگی‌های حرفه‌ای برای RTX 3080 Fine-tuning
# Professional dependencies for RTX 3080 Fine-tuning

# PyTorch - باید جداگانه نصب شود با CUDA
# برای CUDA 11.8:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# برای CUDA 12.1:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# ============================================================================
# Hugging Face Ecosystem (آخرین نسخه‌ها)
# ============================================================================
transformers>=4.40.0
accelerate>=0.27.0
peft>=0.8.0
bitsandbytes>=0.43.3
datasets>=2.18.0
sentencepiece>=0.1.99
protobuf>=3.20.0
huggingface-hub>=0.20.0
tokenizers>=0.15.0
safetensors>=0.4.0

# ============================================================================
# Training & Optimization
# ============================================================================
scipy>=1.11.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0

# ============================================================================
# Inference & UI
# ============================================================================
gradio>=4.0.0

# ============================================================================
# API (اختیاری)
# ============================================================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.31.0

# ============================================================================
# Image Processing (برای توصیف عکس - اختیاری)
# ============================================================================
# pillow>=10.0.0
# torchvision>=0.16.0

# ============================================================================
# Flash Attention (اختیاری - برای سرعت بیشتر)
# ============================================================================
# flash-attn>=2.5.0  # نیاز به CUDA و compilation دارد
# برای نصب: pip install flash-attn --no-build-isolation
