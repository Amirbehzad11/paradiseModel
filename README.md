# سیستم چت بات معنوی فارسی/انگلیسی
# Persian/English Spiritual Chatbot System

سیستم کامل برای آموزش و اجرای یک چت بات معنوی که فقط درباره مرگ، زندگی پس از مرگ، بهشت و آرامش روح صحبت می‌کند.

Complete system for training and running a spiritual chatbot that only talks about death, afterlife, heaven, and spiritual peace.

## 📋 نیازمندی‌ها
## Requirements

- Ubuntu 20.04/22.04
- NVIDIA GPU با 10-24GB VRAM (RTX 3080/3090/A4000/A6000)
- Python 3.8+
- CUDA 11.8+
- دسترسی به Hugging Face (برای دانلود مدل پایه)

## 🚀 نصب سریع
## Quick Installation

```bash
# 1. نصب وابستگی‌ها
chmod +x setup.sh && ./setup.sh

# 2. فعال‌سازی محیط مجازی
source venv/bin/activate

# 3. آموزش مدل (فقط یک بار)
python train_once.py

# 4. شروع چت
python chat.py
```

## 📁 ساختار فایل‌ها
## File Structure

- `setup.sh` - اسکریپت نصب یک‌دستوری
- `train_once.py` - آموزش مدل (یک بار اجرا می‌شود)
- `chat.py` - چت بات ترمینالی
- `dataset.json` - 450 نمونه آموزشی
- `requirements.txt` - وابستگی‌های Python

## 🔧 جزئیات فنی
## Technical Details

### مدل پایه
### Base Model
- `meta-llama/Llama-3.2-1B-Instruct` یا `meta-llama/Llama-3.2-3B-Instruct`

### روش آموزش
### Training Method
- QLoRA با 4-bit quantization
- BitsAndBytes برای بهینه‌سازی حافظه
- LoRA با r=16, alpha=32

### دیتاست
### Dataset
- 150 نمونه معنوی (مرگ، بهشت، روح)
- 150 نمونه رد کردن (با جمله دقیق فارسی)
- 300 نمونه گفتگو با عزیزان فوت‌شده (اول شخص مفرد)

## 📝 استفاده
## Usage

### آموزش مدل
### Training Model

```bash
python train_once.py
```

این اسکریپت:
- بررسی می‌کند آیا مدل از قبل آموزش دیده است
- اگر `./final_model` وجود داشته باشد، آموزش را رد می‌کند
- در غیر این صورت، مدل را آموزش می‌دهد و در `./final_model` ذخیره می‌کند

### چت با مدل
### Chatting with Model

```bash
python chat.py
```

- حلقه بی‌نهایت چت
- برای خروج: `خروج` یا `exit` تایپ کنید
- مدل به صورت خودکار بارگذاری می‌شود

## ⚙️ تنظیمات
## Configuration

### Hugging Face Token
برای دانلود مدل پایه، یکی از این روش‌ها را استفاده کنید:

```bash
# روش 1: متغیر محیطی
export HF_TOKEN=your_token_here

# روش 2: لاگین دستی
huggingface-cli login
```

### تغییر مدل پایه
### Change Base Model

در `train_once.py` و `chat.py`، خط زیر را تغییر دهید:

```python
BASE_MODEL = "meta-llama/Llama-3.2-1B-Instruct"  # یا 3B
```

## 🎯 ویژگی‌ها
## Features

- ✅ آموزش یک‌باره (اگر مدل وجود داشته باشد، رد می‌شود)
- ✅ چت ترمینالی همیشه‌روشن
- ✅ 100% آفلاین پس از اولین دانلود
- ✅ رد کردن خودکار موضوعات غیرمرتبط
- ✅ پشتیبانی کامل از فارسی و انگلیسی

## ⚠️ نکات مهم
## Important Notes

1. **اولین اجرا**: نیاز به اینترنت برای دانلود مدل پایه
2. **پس از آموزش**: کاملاً آفلاین کار می‌کند
3. **حافظه**: حداقل 10GB VRAM برای 1B، 16GB+ برای 3B
4. **زمان آموزش**: حدود 1-3 ساعت بسته به GPU

## 🐛 عیب‌یابی
## Troubleshooting

### خطای CUDA
```bash
# بررسی CUDA
nvidia-smi
```

### خطای حافظه
- از مدل 1B استفاده کنید
- batch_size را در `train_once.py` کاهش دهید

### خطای Hugging Face
```bash
# لاگین مجدد
huggingface-cli login
```

## 📄 مجوز
## License

این پروژه برای استفاده شخصی است.

This project is for personal use.

---

**نکته**: این سیستم فقط درباره موضوعات معنوی (مرگ، بهشت، روح) صحبت می‌کند و هر موضوع دیگری را رد می‌کند.

**Note**: This system only talks about spiritual topics (death, heaven, soul) and refuses all other topics.

